#include<stdio.h>
int main(){
int num;
printf("Enter number: ");
scanf("%d", &num);
if ((num > 0) && (num%2 == 0) && (num%10 == 0)){
	printf("Number is accepted");}
else 
{printf("Number is not accepted");}
return 0;
}
